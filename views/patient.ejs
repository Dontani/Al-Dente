<%- include('partials/header') -%>
<div class="container">
  <div class="row justify-content-center mt-5">
    <div class="col-6">
      <h2><%= patient.title %></h2>
      <img class="img-fluid" src="<%= patient.image%>" />
      <div class="row justify-content-between">
        <form
          class="col-1"
          action="/patient/likepatient/<%= patient.id %>?_method=PUT"
          method="post"
        >
          <button class="btn btn-primary fa fa-heart" type="submit"></button>
        </form>
        <h3 class="col-3">Likes: <%= patient.likes %></h3>
        <%if(patient.user == user.id){ %>
        <form
          action="/post/deletepatient/<%= patient.id %>?_method=DELETE"
          method="post"
          class="col-3"
        >
          <button class="btn btn-primary fa fa-trash" type="submit"></button>
        </form>
        <%}%>
      </div>
    </div>
    <div class="col-3 mt-5">
      <p><%= patient.caption %></p>
    </div>
    <div class="mt-5">
      <h2>Add a Comment</h2>
      <form action="/Comment/createComment/<%=patient._id%>" method="post">
        <div class="mb-3">
            <label for="comment" class="form-label">Comment</label>
            <input type="text" class="form-control" id="comment" name="comment">
          </div>
            <button type="submit" class="btn btn-primary" value="Upload">Submit</button>
          </form>
        </div>
          <ul>
          <% for(var i=0; i<comments.length; i++) {%>
            <li class="col-6 justify-content-between mt-5">
              <%= comments[i].comment %>>
  
            </li>
          <% } %>
          </ul>
    <div class="col-6 mt-5">
      <a class="btn btn-primary" href="/profile">Return to Profile</a>
      <a class="btn btn-primary" href="/feed">Return to Feed</a>
    </div>
  </div>
</div>

<%- include('partials/footer') -%>
